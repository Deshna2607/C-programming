#include <stdio.h>

int main() {
    // Write C code here
    int a,b,gcd;
    scanf("%d%d",&a,&b);
    while(b!=0){
        int temp = b;
        b = a%b;
        a = temp;
    }
    printf("%d",a);

    return 0;
}
