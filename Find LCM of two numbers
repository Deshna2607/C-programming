#include <stdio.h>

int main() {
    // Write C code here
    int a,b,lcm;
    scanf("%d%d",&a,&b);
    int max = a>b? a:b;
    lcm = max;
    while(1){
        if(lcm %a==0 && lcm %b==0)
         break;
         lcm++;
    }
    printf("%d",lcm);

    return 0;
}
