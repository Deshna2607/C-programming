#include <stdio.h>
int main() {
    // Write C code here
    int num,digit,freq=0;
    scanf("%d%d",&num,&digit);
    while(num>0){
        if(num%10==digit)freq++;
        num/=10;
    }
    printf("%d",freq);
    return 0;
}
