#include <stdio.h>
int main() {
    // Write C code here
    int n,a,r,i=1;
    scanf("%d%d%d",&a,&r,&n);
    while(i<=n) {
         printf("%d",a);
         a*=r;
         i++;
    }
    return 0;
}
